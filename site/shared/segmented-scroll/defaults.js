export const DEFAULT_TIMINGS = Object.freeze({
  navigationLockMs: 720,
  settleMs: 640,
  wheelThreshold: 86,
  wheelResetMs: 180,
  touchThreshold: 48,
  touchIntentThreshold: 10,
  touchMomentumSettleMs: 520,
  alignmentFrames: 36,
  touchActivityStaleMs: 1100,
  verticalIntentRatio: 1.25,
  zoomThreshold: 1.02
});

export const DEFAULT_OBSERVER_THRESHOLDS = Object.freeze([0.42, 0.58, 0.72]);
