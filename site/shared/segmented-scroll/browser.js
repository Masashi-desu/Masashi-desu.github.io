// Generated by npm run build:segmented-scroll. Edit the ESM sources, not this file.
var MDWSegmentedScroll = (function(exports) {
	Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
	//#region site/shared/segmented-scroll/defaults.js
	const DEFAULT_TIMINGS = Object.freeze({
		navigationLockMs: 720,
		settleMs: 640,
		wheelThreshold: 86,
		wheelResetMs: 180,
		touchThreshold: 48,
		touchIntentThreshold: 10,
		touchMomentumSettleMs: 520,
		alignmentFrames: 36,
		touchActivityStaleMs: 1100,
		verticalIntentRatio: 1.25,
		zoomThreshold: 1.02
	});
	const DEFAULT_OBSERVER_THRESHOLDS = Object.freeze([
		.42,
		.58,
		.72
	]);
	//#endregion
	//#region site/shared/segmented-scroll/stop-index.js
	function createStopIndex(options = {}) {
		let stops = [];
		let activeId = options.initialId || "";
		let activeContentId = options.initialContentId || "";
		function setStops(nextStops) {
			const normalized = Array.isArray(nextStops) ? nextStops.filter(Boolean) : [];
			const ids = /* @__PURE__ */ new Set();
			normalized.forEach((stop) => {
				if (!stop || typeof stop.id !== "string" || !stop.id) throw new TypeError("Each segmented scroll stop requires a non-empty string id.");
				if (ids.has(stop.id)) throw new Error(`Duplicate segmented scroll stop id: ${stop.id}`);
				ids.add(stop.id);
			});
			stops = normalized.map((stop) => Object.freeze({ ...stop }));
			const fallback = getById(options.initialId) || getFirstContentStop() || stops[0] || null;
			if (!getById(activeId)) activeId = fallback ? fallback.id : "";
			const activeContent = getById(activeContentId);
			if (!activeContent || getRole(activeContent) !== "content") {
				const active = getById(activeId);
				activeContentId = active && getRole(active) === "content" ? active.id : (getFirstContentStop() || {}).id || "";
			}
			if (activeId) activate(activeId);
			return getState();
		}
		function getStops() {
			return stops.slice();
		}
		function getById(id) {
			return stops.find((stop) => stop.id === id) || null;
		}
		function getFirstContentStop() {
			return stops.find((stop) => getRole(stop) === "content") || null;
		}
		function activate(id) {
			const stop = getById(id);
			if (!stop) return null;
			activeId = stop.id;
			if (getRole(stop) === "content") activeContentId = stop.id;
			else {
				const anchoredContent = resolveContentAnchor(stop);
				if (anchoredContent) activeContentId = anchoredContent.id;
			}
			return stop;
		}
		function resolveContentAnchor(stop) {
			if (typeof stop.contentAnchor === "function") {
				const requested = getById(stop.contentAnchor({
					stop,
					stops: getStops(),
					activeContentId
				}));
				return requested && getRole(requested) === "content" ? requested : null;
			}
			if (typeof stop.contentAnchor === "string" && stop.contentAnchor !== "previous") {
				const requested = getById(stop.contentAnchor);
				return requested && getRole(requested) === "content" ? requested : null;
			}
			if (stop.contentAnchor !== "previous") return null;
			const stopIndex = stops.indexOf(stop);
			for (let index = stopIndex - 1; index >= 0; index -= 1) if (getRole(stops[index]) === "content") return stops[index];
			return null;
		}
		function getOrderedStops(readTop) {
			if (typeof readTop !== "function") return stops.slice();
			return stops.map((stop, order) => ({
				...stop,
				top: readTop(stop),
				__order: order
			})).filter((stop) => Number.isFinite(stop.top)).sort((left, right) => left.top - right.top || left.__order - right.__order).map(({ __order, ...stop }) => stop);
		}
		function findDirectional(direction, currentTop, preferredId, readTop) {
			if (direction === 0) return null;
			const ordered = getOrderedStops(readTop);
			if (preferredId) {
				const preferredIndex = ordered.findIndex((stop) => stop.id === preferredId);
				if (preferredIndex >= 0) return ordered[preferredIndex + direction] || null;
			}
			const tolerance = 1;
			if (direction > 0) return ordered.find((stop) => stop.top > currentTop + tolerance) || null;
			for (let index = ordered.length - 1; index >= 0; index -= 1) if (ordered[index].top < currentTop - tolerance) return ordered[index];
			return null;
		}
		function findNearest(currentTop, readTop) {
			const ordered = getOrderedStops(readTop);
			let nearest = null;
			let distance = Infinity;
			ordered.forEach((stop) => {
				const candidateDistance = Math.abs(stop.top - currentTop);
				if (candidateDistance < distance) {
					nearest = stop;
					distance = candidateDistance;
				}
			});
			return nearest ? {
				stop: nearest,
				distance
			} : null;
		}
		function getState() {
			return {
				activeId,
				activeIndex: stops.findIndex((stop) => stop.id === activeId),
				activeContentId,
				activeContentIndex: stops.findIndex((stop) => stop.id === activeContentId),
				size: stops.length
			};
		}
		return Object.freeze({
			setStops,
			getStops,
			getById,
			getOrderedStops,
			findDirectional,
			findNearest,
			activate,
			getState
		});
	}
	function getRole(stop) {
		return stop && stop.role === "auxiliary" ? "auxiliary" : "content";
	}
	//#endregion
	//#region site/shared/segmented-scroll/window-scroll-adapter.js
	function createWindowScrollAdapter(win, doc) {
		function getViewportHeight() {
			const viewport = win.visualViewport;
			return Math.round(viewport && viewport.height ? viewport.height : win.innerHeight || doc.documentElement.clientHeight);
		}
		function getScrollY() {
			return Number.isFinite(win.scrollY) ? win.scrollY : doc.documentElement.scrollTop || 0;
		}
		function getDocumentBottom() {
			const documentHeight = Math.max(doc.body ? doc.body.scrollHeight : 0, doc.documentElement.scrollHeight);
			return Math.max(0, Math.round(documentHeight - getViewportHeight()));
		}
		function readStopTop(stop) {
			if (!stop) return null;
			const context = {
				window: win,
				document: doc,
				stop,
				viewportHeight: getViewportHeight(),
				scrollY: getScrollY(),
				documentBottom: getDocumentBottom()
			};
			if (typeof stop.getTop === "function") {
				const value = stop.getTop(context);
				const customTop = value === null ? NaN : Number(value);
				return Number.isFinite(customTop) ? customTop : null;
			}
			if (stop.align === "document-end") return context.documentBottom;
			if (!stop.element || typeof stop.element.getBoundingClientRect !== "function") return null;
			const rect = stop.element.getBoundingClientRect();
			if (stop.align === "end") return Math.max(0, Math.round(rect.bottom + context.scrollY - context.viewportHeight));
			return Math.max(0, Math.round(rect.top + context.scrollY));
		}
		return {
			getViewportHeight,
			getScrollY,
			getDocumentBottom,
			readStopTop,
			scrollTo: (top, behavior = "auto") => win.scrollTo({
				top,
				behavior
			})
		};
	}
	//#endregion
	//#region site/shared/segmented-scroll/scroll-controller.js
	const mountedWindows = /* @__PURE__ */ new WeakMap();
	function createScrollController(options = {}) {
		const win = options.window || (typeof window !== "undefined" ? window : null);
		const doc = options.document || (win ? win.document : null);
		if (!win || !doc) throw new TypeError("createScrollController requires a window and document.");
		const timings = {
			...DEFAULT_TIMINGS,
			...options.timings || {}
		};
		const reduceMotion = options.reduceMotion || win.matchMedia("(prefers-reduced-motion: reduce)");
		const index = createStopIndex({ initialId: options.initialId });
		const adapter = createWindowScrollAdapter(win, doc);
		const { getViewportHeight, getScrollY, readStopTop } = adapter;
		const subscribers = /* @__PURE__ */ new Set();
		const eventQueue = [];
		let emitting = false;
		const ownedClasses = /* @__PURE__ */ new Set();
		const rootElement = options.rootElement || doc.documentElement;
		const eventTarget = options.eventTarget || win;
		const observerThresholds = options.observerThresholds || DEFAULT_OBSERVER_THRESHOLDS;
		const listenerDisposers = [];
		let mounted = false;
		let observer = null;
		let navigationLockUntil = 0;
		let pendingId = "";
		let settledId = "";
		let navigation = null;
		let navigationSequence = 0;
		let navigationLockTimer = null;
		let previousLocks = "";
		let settleTimer = null;
		let alignmentFrame = null;
		let restTimer = null;
		let restSampleScrollY = null;
		let touchPointsActive = 0;
		let lastTouchEventAt = 0;
		let wheelDeltaY = 0;
		let wheelResetTimer = null;
		let lastWheelEventAt = null;
		let wheelHandled = false;
		let touchStartX = 0;
		let touchStartY = 0;
		let touchHandled = false;
		let touchStartStopId = "";
		function getStops() {
			return typeof options.getStops === "function" ? options.getStops() : [];
		}
		function activateStop(stopOrId, source = "programmatic", force = false) {
			const id = typeof stopOrId === "string" ? stopOrId : stopOrId && stopOrId.id;
			const previousId = index.getState().activeId;
			const stop = index.activate(id);
			if (!stop) return null;
			if (force || previousId !== stop.id) emit("activechange", {
				source,
				stop
			});
			if (index.getState().activeId === stop.id && (force || previousId !== stop.id) && typeof options.onActiveChange === "function") options.onActiveChange({
				source,
				stop,
				...index.getState()
			});
			return stop;
		}
		function subscribe(listener) {
			if (typeof listener !== "function") throw new TypeError("A subscriber must be a function.");
			subscribers.add(listener);
			return () => subscribers.delete(listener);
		}
		function emit(type, detail = {}) {
			const event = Object.freeze({
				...detail,
				type,
				state: Object.freeze(getState())
			});
			eventQueue.push({
				event,
				listeners: [...subscribers]
			});
			if (emitting) return;
			emitting = true;
			try {
				while (eventQueue.length > 0) {
					const next = eventQueue.shift();
					for (const listener of next.listeners) listener(next.event);
				}
			} finally {
				eventQueue.length = 0;
				emitting = false;
			}
		}
		function notifyLocks() {
			const locks = `${isNavigationLocked()}:${wheelHandled}:${touchHandled}`;
			if (locks !== previousLocks) {
				previousLocks = locks;
				emit("lockchange");
			}
		}
		function syncManagedClasses() {
			for (const name of ["segmented-scroll-managed", options.managedClass].filter(Boolean)) if (mounted && index.getStops().length > 0) {
				if (rootElement?.classList && !rootElement.classList.contains(name)) {
					rootElement.classList.add(name);
					ownedClasses.add(name);
				}
			} else if (ownedClasses.delete(name)) rootElement.classList.remove(name);
		}
		function refresh() {
			index.setStops(getStops());
			if (navigation && !index.getById(navigation.id)) cancelNavigation("stop-removed");
			if (!index.getById(settledId)) settledId = "";
			const activeTop = readStopTop(index.getById(index.getState().activeId));
			if (!navigation && activeTop !== null && Math.abs(getScrollY() - activeTop) <= 1) settledId = index.getState().activeId;
			syncManagedClasses();
			setupObserver();
			if (index.getState().activeId) activateStop(index.getState().activeId, "refresh", true);
			else emit("activechange", {
				source: "refresh",
				stop: null
			});
			return getState();
		}
		function setupObserver() {
			if (observer) {
				observer.disconnect();
				observer = null;
			}
			if (!mounted || !("IntersectionObserver" in win)) return;
			const observedStops = index.getStops().filter((stop) => stop.element && stop.observe !== false && getRole(stop) === "content");
			if (observedStops.length === 0) return;
			observer = new win.IntersectionObserver((entries) => {
				entries.forEach((entry) => {
					if (options.visibleClass && entry.target.classList) entry.target.classList.toggle(options.visibleClass, entry.isIntersecting);
					if (typeof options.onVisibilityChange === "function") {
						const stop = index.getStops().find((candidate) => candidate.element === entry.target) || null;
						options.onVisibilityChange({
							entry,
							stop,
							visible: entry.isIntersecting
						});
					}
				});
				if (navigation || isNavigationLocked()) return;
				const positionStop = findStopAtCurrentPosition(true);
				if (positionStop) {
					activateStop(positionStop, "position");
					return;
				}
				const visible = entries.filter((entry) => entry.isIntersecting).sort((left, right) => right.intersectionRatio - left.intersectionRatio)[0];
				if (!visible) return;
				const stop = index.getStops().find((candidate) => candidate.element === visible.target);
				if (stop) activateStop(stop, "observer");
			}, { threshold: observerThresholds });
			observedStops.forEach((stop) => observer.observe(stop.element));
		}
		function mount() {
			if (mounted) return getState();
			if (mountedWindows.has(win)) throw new Error("Only one scroll controller may be mounted per window.");
			mountedWindows.set(win, api);
			mounted = true;
			try {
				refresh();
			} catch (error) {
				destroy();
				throw error;
			}
			if (!mounted) return getState();
			listen(eventTarget, "wheel", handleWheel, { passive: false });
			listen(eventTarget, "scroll", handleScroll, { passive: true });
			listen(eventTarget, "touchstart", handleTouchStart, { passive: true });
			listen(eventTarget, "touchmove", handleTouchMove, { passive: false });
			listen(eventTarget, "touchend", handleTouchEnd, { passive: true });
			listen(eventTarget, "touchcancel", handleTouchEnd, { passive: true });
			return getState();
		}
		function destroy() {
			mounted = false;
			navigationSequence += 1;
			if (mountedWindows.get(win) === api) mountedWindows.delete(win);
			cancelNavigation("destroyed");
			listenerDisposers.splice(0).forEach((dispose) => dispose());
			if (observer) {
				observer.disconnect();
				observer = null;
			}
			clearSettleTimer();
			resetWheelGesture();
			cancelScrollAlignment();
			if (restTimer !== null) {
				win.clearTimeout(restTimer);
				restTimer = null;
			}
			syncManagedClasses();
			if (navigationLockTimer !== null) win.clearTimeout(navigationLockTimer);
			navigationLockTimer = null;
			navigationLockUntil = 0;
			pendingId = "";
			restSampleScrollY = null;
			touchPointsActive = 0;
			lastTouchEventAt = 0;
			touchHandled = false;
			touchStartStopId = "";
			settledId = "";
			notifyLocks();
			emit("destroy");
			subscribers.clear();
		}
		function listen(target, type, handler, listenerOptions) {
			if (!target || typeof target.addEventListener !== "function") return;
			target.addEventListener(type, handler, listenerOptions);
			listenerDisposers.push(() => target.removeEventListener(type, handler, listenerOptions));
		}
		function prefersReducedMotion() {
			return Boolean(reduceMotion && reduceMotion.matches);
		}
		function isNavigationLocked() {
			return Date.now() < navigationLockUntil;
		}
		function lockFor(duration) {
			navigationLockUntil = Date.now() + duration;
			if (navigationLockTimer !== null) win.clearTimeout(navigationLockTimer);
			navigationLockTimer = win.setTimeout(() => {
				navigationLockTimer = null;
				notifyLocks();
			}, duration);
			notifyLocks();
		}
		function clearWheelAccumulation() {
			wheelDeltaY = 0;
		}
		function resetWheelGesture() {
			clearWheelAccumulation();
			wheelHandled = false;
			lastWheelEventAt = null;
			notifyLocks();
			if (wheelResetTimer !== null) {
				win.clearTimeout(wheelResetTimer);
				wheelResetTimer = null;
			}
		}
		function clearSettleTimer() {
			if (settleTimer !== null) {
				win.clearTimeout(settleTimer);
				settleTimer = null;
			}
		}
		function cancelScrollAlignment() {
			if (alignmentFrame !== null) {
				win.cancelAnimationFrame(alignmentFrame);
				alignmentFrame = null;
			}
		}
		function cancelNavigation(reason) {
			clearSettleTimer();
			cancelScrollAlignment();
			const cancelled = navigation;
			navigation = null;
			pendingId = "";
			if (navigationLockTimer !== null) win.clearTimeout(navigationLockTimer);
			navigationLockTimer = null;
			navigationLockUntil = 0;
			if (cancelled) {
				emit("navigationcancel", {
					id: cancelled.id,
					source: cancelled.source,
					reason
				});
				notifyLocks();
			}
		}
		function finishNavigation(current) {
			if (navigation !== current) return;
			const stop = index.getById(current.id);
			settledId = current.id;
			navigation = null;
			pendingId = "";
			emit("navigationend", {
				id: current.id,
				source: current.source,
				stop
			});
		}
		function enforceScrollAlignment(current) {
			cancelScrollAlignment();
			let framesLeft = Math.max(1, timings.alignmentFrames);
			const step = () => {
				alignmentFrame = null;
				if (!mounted || navigation !== current) return;
				const targetTop = readStopTop(index.getById(current.id));
				if (targetTop === null) {
					cancelNavigation("stop-unavailable");
					return;
				}
				if (Math.abs(getScrollY() - targetTop) > 1) adapter.scrollTo(targetTop);
				framesLeft -= 1;
				if (framesLeft > 0) alignmentFrame = win.requestAnimationFrame(step);
				else if (Math.abs(getScrollY() - targetTop) <= 1) finishNavigation(current);
				else cancelNavigation("alignment-failed");
			};
			step();
		}
		function settle(current, delay = timings.settleMs) {
			clearSettleTimer();
			settleTimer = win.setTimeout(() => {
				settleTimer = null;
				if (navigation === current) enforceScrollAlignment(current);
			}, prefersReducedMotion() ? 80 : delay);
		}
		function goTo(stopOrId, navigationOptions = {}) {
			const stop = index.getById(typeof stopOrId === "string" ? stopOrId : stopOrId?.id);
			const top = readStopTop(stop);
			if (!mounted || !stop || top === null) return false;
			const sequence = ++navigationSequence;
			cancelNavigation("superseded");
			if (!mounted || sequence !== navigationSequence) return false;
			const current = {
				id: stop.id,
				source: navigationOptions.source || "programmatic",
				sequence
			};
			navigation = current;
			pendingId = stop.id;
			lockFor(prefersReducedMotion() ? 160 : Math.max(timings.navigationLockMs, timings.settleMs + 140));
			if (navigation !== current) return false;
			activateStop(stop, current.source);
			if (navigation !== current) return false;
			emit("navigationstart", {
				id: stop.id,
				source: current.source,
				stop
			});
			if (navigation !== current) return false;
			if (typeof options.onNavigate === "function") options.onNavigate({
				stop,
				options: navigationOptions,
				state: getState()
			});
			if (navigation !== current) return false;
			const behavior = navigationOptions.behavior || (prefersReducedMotion() ? "auto" : "smooth");
			if (typeof stop.scroll === "function") stop.scroll({
				behavior,
				top,
				window: win,
				document: doc,
				stop
			});
			else adapter.scrollTo(top, behavior);
			if (navigation === current) settle(current, navigationOptions.settleMs ?? timings.settleMs);
			return true;
		}
		function move(direction, navigationOptions = {}) {
			const preferredId = navigationOptions.fromId || index.getState().activeId;
			const target = index.findDirectional(direction, getScrollY(), preferredId, readStopTop);
			return target ? goTo(target.id, navigationOptions) : false;
		}
		function getDirectionalStop(direction, event, preferredId = "") {
			const anchorId = preferredId || getEventStopId(event) || findStopAtCurrentPosition(false, getPositionTolerance())?.id || "";
			return index.findDirectional(direction, getScrollY(), anchorId, readStopTop);
		}
		function navigateDirection(direction, event, preferredId = "") {
			const target = getDirectionalStop(direction, event, preferredId);
			if (!target) {
				clearWheelAccumulation();
				return false;
			}
			if (event && event.cancelable) event.preventDefault();
			if (isNavigationLocked()) return true;
			clearWheelAccumulation();
			return goTo(target.id, {
				source: event ? event.type : "direction",
				updateHistory: false
			});
		}
		function normalizeWheelDelta(event) {
			if (event.deltaMode === 1) return event.deltaY * 16;
			if (event.deltaMode === 2) return event.deltaY * getViewportHeight();
			return event.deltaY;
		}
		function handleWheel(event) {
			if (event.ctrlKey || win.visualViewport && win.visualViewport.scale > timings.zoomThreshold) return;
			const gesture = {
				deltaX: event.deltaX,
				deltaY: event.deltaY,
				horizontalDominant: Math.abs(event.deltaX) > Math.abs(event.deltaY),
				shiftKey: event.shiftKey
			};
			if (typeof options.shouldYieldWheel === "function" && options.shouldYieldWheel(event, gesture)) return;
			const deltaY = normalizeWheelDelta(event);
			if (!Number.isFinite(deltaY) || index.getStops().length === 0) return;
			if (deltaY === 0 && event.deltaX !== 0) return;
			const now = Date.now();
			if (lastWheelEventAt !== null && now - lastWheelEventAt >= timings.wheelResetMs) {
				resetWheelGesture();
				if (!mounted) return;
			}
			lastWheelEventAt = now;
			if (wheelResetTimer !== null) win.clearTimeout(wheelResetTimer);
			wheelResetTimer = win.setTimeout(resetWheelGesture, timings.wheelResetMs);
			if (!event.cancelable) {
				wheelHandled = true;
				notifyLocks();
				clearWheelAccumulation();
				return;
			}
			event.preventDefault();
			if (isNavigationLocked()) {
				wheelHandled = true;
				notifyLocks();
			}
			if (wheelHandled || deltaY === 0) return;
			const direction = deltaY > 0 ? 1 : -1;
			if (!getDirectionalStop(direction, event)) {
				clearWheelAccumulation();
				return;
			}
			if (wheelDeltaY !== 0 && Math.sign(wheelDeltaY) !== direction) wheelDeltaY = 0;
			wheelDeltaY += deltaY;
			if (Math.abs(wheelDeltaY) >= timings.wheelThreshold) {
				wheelHandled = true;
				navigateDirection(direction, event);
				notifyLocks();
			}
		}
		function markTouchActivity(event) {
			lastTouchEventAt = Date.now();
			touchPointsActive = event && event.touches ? event.touches.length : 0;
		}
		function handleTouchStart(event) {
			markTouchActivity(event);
			scheduleRestAlignmentCheck(timings.touchActivityStaleMs + 320);
			if (!event.touches || event.touches.length !== 1) {
				touchStartStopId = "";
				return;
			}
			touchStartX = event.touches[0].clientX;
			touchStartY = event.touches[0].clientY;
			touchHandled = false;
			notifyLocks();
			touchStartStopId = getEventStopId(event);
		}
		function handleTouchMove(event) {
			markTouchActivity(event);
			if (!event.touches || event.touches.length !== 1) return;
			const touch = event.touches[0];
			const deltaX = touchStartX - touch.clientX;
			const deltaY = touchStartY - touch.clientY;
			const absDeltaX = Math.abs(deltaX);
			const absDeltaY = Math.abs(deltaY);
			const gesture = {
				deltaX,
				deltaY,
				absDeltaX,
				absDeltaY,
				horizontalDominant: absDeltaX > absDeltaY,
				verticalIntent: absDeltaY >= timings.touchIntentThreshold && absDeltaY >= absDeltaX * timings.verticalIntentRatio
			};
			if (typeof options.shouldYieldTouch === "function" && options.shouldYieldTouch(event, gesture)) return;
			if (event.cancelable) event.preventDefault();
			if (!gesture.verticalIntent) return;
			const direction = deltaY > 0 ? 1 : -1;
			if (!getDirectionalStop(direction, event, touchStartStopId)) return;
			if (touchHandled || absDeltaY < timings.touchThreshold) return;
			touchHandled = true;
			navigateDirection(direction, event, touchStartStopId);
			notifyLocks();
		}
		function handleTouchEnd(event) {
			markTouchActivity(event);
			if (touchHandled && navigation) {
				lockFor(prefersReducedMotion() ? 160 : timings.touchMomentumSettleMs + 140);
				cancelScrollAlignment();
				settle(navigation, timings.touchMomentumSettleMs);
			}
			touchHandled = false;
			touchStartStopId = "";
			notifyLocks();
			if (touchPointsActive === 0) scheduleRestAlignmentCheck(timings.touchMomentumSettleMs + 200);
		}
		function getEventStopId(event) {
			if (!event) return "";
			const stop = index.getStops().find((candidate) => {
				const region = candidate.eventRegion || null;
				return region && eventIncludesElement(event, region);
			});
			return stop ? stop.id : "";
		}
		function handleScroll() {
			if (!navigation && !isNavigationLocked()) {
				const stop = findStopAtCurrentPosition(false);
				if (stop) {
					if (Math.abs(getScrollY() - readStopTop(stop)) <= 1) settledId = stop.id;
					activateStop(stop, "position");
				}
			}
			if (shouldMonitorRestAlignment()) scheduleRestAlignmentCheck(260);
		}
		function findStopAtCurrentPosition(auxiliaryOnly, maxDistance = Infinity) {
			const current = getScrollY();
			let match = null;
			let matchDistance = Infinity;
			index.getOrderedStops(readStopTop).forEach((stop) => {
				if (auxiliaryOnly && getRole(stop) === "content") return;
				const distance = Math.abs(stop.top - current);
				if (distance <= Math.min(getActivationTolerance(stop), maxDistance) && distance < matchDistance) {
					match = stop;
					matchDistance = distance;
				}
			});
			return match;
		}
		function getActivationTolerance(stop) {
			if (typeof stop.activationTolerance === "function") return Number(stop.activationTolerance({
				stop,
				viewportHeight: getViewportHeight()
			})) || 0;
			if (Number.isFinite(stop.activationTolerance)) return stop.activationTolerance;
			if (getRole(stop) !== "content" && stop.element) return Math.max(12, stop.element.offsetHeight * .4);
			return getPositionTolerance();
		}
		function getPositionTolerance() {
			return Math.max(4, Math.round(getViewportHeight() * .02));
		}
		function shouldMonitorRestAlignment() {
			if (typeof options.monitorRestAlignment === "boolean") return options.monitorRestAlignment;
			return "ontouchstart" in win || win.matchMedia("(any-pointer: coarse)").matches;
		}
		function scheduleRestAlignmentCheck(delay) {
			if (!shouldMonitorRestAlignment()) return;
			if (restTimer !== null) win.clearTimeout(restTimer);
			restTimer = win.setTimeout(runRestAlignmentCheck, delay);
		}
		function runRestAlignmentCheck() {
			restTimer = null;
			if (touchPointsActive > 0 && Date.now() - lastTouchEventAt < timings.touchActivityStaleMs || isNavigationLocked() || pendingId || alignmentFrame !== null) {
				restSampleScrollY = null;
				scheduleRestAlignmentCheck(320);
				return;
			}
			touchPointsActive = 0;
			if (win.visualViewport && win.visualViewport.scale > timings.zoomThreshold || index.getStops().length === 0) return;
			const current = getScrollY();
			if (restSampleScrollY === null || Math.abs(restSampleScrollY - current) > 2) {
				restSampleScrollY = current;
				scheduleRestAlignmentCheck(220);
				return;
			}
			restSampleScrollY = null;
			const nearest = index.findNearest(current, readStopTop);
			if (!nearest) return;
			const tolerance = getPositionTolerance();
			if (nearest.distance > tolerance) goTo(nearest.stop.id, {
				source: "rest",
				behavior: "auto",
				settleMs: 0,
				updateHistory: false
			});
			else {
				if (nearest.distance <= 1) settledId = nearest.stop.id;
				activateStop(nearest.stop, "rest");
			}
		}
		function getState() {
			return {
				...index.getState(),
				pendingId,
				targetId: navigation?.id || "",
				settledId,
				moving: navigation !== null,
				navigationLocked: isNavigationLocked(),
				wheelLocked: wheelHandled,
				touchLocked: touchHandled,
				locked: isNavigationLocked() || wheelHandled || touchHandled,
				mounted,
				aligning: alignmentFrame !== null
			};
		}
		const api = Object.freeze({
			subscribe,
			getStop: index.getById,
			getStops: index.getStops,
			mount,
			refresh,
			destroy,
			goTo,
			move,
			setActive: activateStop,
			readStopTop,
			getState
		});
		return api;
	}
	function eventIncludesElement(event, element) {
		if (!event || !element) return false;
		if (typeof event.composedPath === "function") return event.composedPath().includes(element);
		const EventNode = element.ownerDocument && element.ownerDocument.defaultView ? element.ownerDocument.defaultView.Node : typeof Node !== "undefined" ? Node : null;
		return Boolean(EventNode && event.target instanceof EventNode && element.contains(event.target));
	}
	//#endregion
	//#region site/shared/segmented-scroll/segment-view.js
	function createSegmentView(options = {}) {
		let activeId = "";
		let disconnect = () => {};
		const track = options.track || null;
		const activeClass = options.activeClass || "is-active";
		const ariaAttribute = options.ariaAttribute || "aria-current";
		const xProperty = options.xProperty || "--segment-x";
		const widthProperty = options.widthProperty || "--segment-width";
		function getControls() {
			const controls = typeof options.getControls === "function" ? options.getControls() : options.controls;
			return Array.isArray(controls) ? controls.filter(Boolean) : Array.from(controls || []).filter(Boolean);
		}
		function getTargetId(control) {
			return typeof options.getTargetId === "function" ? options.getTargetId(control) : control.id;
		}
		function render(targetId = activeId) {
			activeId = targetId;
			let activeControl = null;
			getControls().forEach((control) => {
				const active = getTargetId(control) === targetId;
				if (control.classList) control.classList.toggle(activeClass, active);
				if (typeof control.setAttribute === "function") control.setAttribute(ariaAttribute, active ? "true" : "false");
				if (active) activeControl = control;
			});
			if (activeControl && typeof options.revealControl === "function") options.revealControl(activeControl, { targetId });
			updateIndicator(activeControl);
			return activeControl;
		}
		function connect(controller) {
			disconnect();
			render(controller.getState().activeId);
			const unsubscribe = controller.subscribe((event) => {
				if (event.type === "activechange") render(event.state.activeId);
				if (event.type === "destroy") disconnect();
			});
			const win = options.window || track?.ownerDocument?.defaultView;
			const update = () => updateIndicator();
			win?.addEventListener?.("resize", update);
			track?.addEventListener?.("scroll", update, {
				capture: true,
				passive: true
			});
			const release = () => {
				unsubscribe();
				win?.removeEventListener?.("resize", update);
				track?.removeEventListener?.("scroll", update, true);
			};
			disconnect = release;
			return release;
		}
		function updateIndicator(control) {
			const activeControl = control || getControls().find((candidate) => getTargetId(candidate) === activeId);
			if (!track || !activeControl || !track.style) return;
			const measurement = typeof options.measureIndicator === "function" ? options.measureIndicator(activeControl, track) : measureRelativeRect(activeControl, track);
			if (!measurement || !Number.isFinite(measurement.x) || !Number.isFinite(measurement.width)) return;
			track.style.setProperty(xProperty, `${measurement.x}px`);
			track.style.setProperty(widthProperty, `${measurement.width}px`);
		}
		return Object.freeze({
			connect,
			destroy: () => disconnect(),
			render,
			updateIndicator
		});
	}
	function measureRelativeRect(control, track) {
		if (typeof control.getBoundingClientRect !== "function" || typeof track.getBoundingClientRect !== "function") return null;
		const controlRect = control.getBoundingClientRect();
		const trackRect = track.getBoundingClientRect();
		const trackBorderLeft = Number.isFinite(track.clientLeft) ? track.clientLeft : 0;
		return {
			x: controlRect.left - trackRect.left - trackBorderLeft,
			width: controlRect.width
		};
	}
	//#endregion
	//#region site/shared/segmented-scroll/viewport.js
	function createViewportCssSync(options = {}) {
		const win = options.window || (typeof window !== "undefined" ? window : null);
		const rootElement = options.rootElement || (win ? win.document.documentElement : null);
		const disposers = [];
		let mounted = false;
		function sync() {
			if (!win || !rootElement) return null;
			const viewport = win.visualViewport;
			const width = Math.round(viewport && viewport.width ? viewport.width : win.innerWidth);
			const height = Math.round(viewport && viewport.height ? viewport.height : win.innerHeight);
			if (options.widthProperty && width > 0) rootElement.style.setProperty(options.widthProperty, `${width}px`);
			if (options.heightProperty && height > 0) rootElement.style.setProperty(options.heightProperty, `${height}px`);
			if (typeof options.onChange === "function") options.onChange({
				width,
				height
			});
			return {
				width,
				height
			};
		}
		function listen(target, type) {
			if (!target || typeof target.addEventListener !== "function") return;
			target.addEventListener(type, sync);
			disposers.push(() => target.removeEventListener(type, sync));
		}
		function mount() {
			if (!win || !rootElement || mounted) return sync();
			mounted = true;
			listen(win, "resize");
			listen(win, "orientationchange");
			listen(win.visualViewport, "resize");
			return sync();
		}
		function destroy() {
			disposers.splice(0).forEach((dispose) => dispose());
			mounted = false;
		}
		return Object.freeze({
			mount,
			sync,
			destroy
		});
	}
	//#endregion
	//#region site/shared/segmented-scroll/index.js
	const version = "0.1.0";
	//#endregion
	exports.DEFAULT_TIMINGS = DEFAULT_TIMINGS;
	exports.createScrollController = createScrollController;
	exports.createSegmentView = createSegmentView;
	exports.createStopIndex = createStopIndex;
	exports.createViewportCssSync = createViewportCssSync;
	exports.version = version;
	return exports;
})({});
